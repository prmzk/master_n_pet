<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Master and Pet</title>
     <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/style-home.css">

</head>
<body>
    <%- include('./includes/navbar.ejs') %> 
    <form action=<%= "/pets/edit/user/" + data.id %> method="POST">
        <label for="name">Name:</label><br>
        <input type="text" id="name" name="name" value= <%= data.name %>><br>
        <label for="species">Species:</label><br>
        <select id="species", name="species">
            <option value="Cat" <%= (data.species == 'Cat') ? 'selected':'' %>>Cat</option>
            <option value="Dog" <%= (data.species == 'Dog') ? 'selected':'' %>>Dog</option>
            <option value="Bunny" <%= (data.species == 'Bunny') ? 'selected':'' %>>Bunny</option>
            <option value="Hamster" <%= (data.species == 'Hamster') ? 'selected':'' %>>Hamster</option>
            <option value="Fish" <%= (data.species == 'Fish') ? 'selected':'' %>>Fish</option>
            <option value="Other" <%= (data.species == 'Other') ? 'selected':'' %>>Other..</option>
        </select><br>
        <label for="breed">Breed:</label><br>
        <input type="text" id="breed" name="breed" value=<%= data.breed %>><br>
        <label for="age">Age:</label><br>
        <input type="text" id="age" name="age" value=<%= data.age %>><br>
        <label for="status">Status:</label><br>
        <select id="status", name="status">
            <option value="for breeding" <%= (data.status == 'for breeding') ? 'selected':'' %>>for breeding</option>
            <option value="for adopting" <%= (data.status == 'for adopting') ? 'selected':'' %>>for adopting</option>
            <option value="unavailable" <%= (data.status == 'unavailable') ? 'selected':'' %>>unavailable</option>
        </select><br>
        <br><br>
        <input type="submit" value="Submit">
      </form> 
      <% if(error) { 
          error.errors.forEach( el => { %>
        <p style="color: red;"><%= el.message %></p>
      <% }) 
    } %>
    <%- include('./includes/bsjs.ejs') %>
</body>
</html>